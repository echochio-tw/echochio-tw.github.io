I"�<p>docker image</p>

<p>docker container</p>

<p>docker tag</p>

<p>docker Repository public</p>

<p>docker Repository private</p>

<p>docker Registry</p>

<h2 id="docker-image-常用的紀錄一下">docker image 常用的紀錄一下</h2>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker search ubuntu
docker search -s 20 ubuntu 
(top 20 ubuntu image)
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull ubuntu 
(拉)
docker pull ubuntu:latest 
(pull with tag(ver) container)

docker images 
( list local images)
(REPOSITORY , TAG ,  IMAGE ID)
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker inspect ubuntu:14.04 
( list ubuntu 14.04 詳細資訊)
(output with json use less to view)
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker save -o ubuntu-latest.tar ubuntu:latest 
(ubuntu:latest backup to local as tar file)
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker load --i ubuntu-latest.tar
(load local file ubuntu-latest.tar to docker container)
</code></pre></div></div>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker rmi ubuntu:14.04 
(rm local image ubuntu:14.04 )
</code></pre></div></div>
:ET