I"�<p>這邊簡單紀錄一下 pyinstaller 這工具在 windows 與 linux 都可正常運作</p>

<p>這是把所需的都包在裡面不是真的編譯</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip install pyinstaller

pyinstaller -F helloword.py

dist\helloword

輸出 : 
hello word

</code></pre></div></div>

<p>要檔案小一點用 upx 壓縮 (大部分 linux 都會有 upx … 可不指定也會壓縮)</p>
<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pyinstaller helloword.py --upx-dir=c:\python -y --onefile

pyinstaller helloword.py --upx-dir=/usr/bin -y --onefile

</code></pre></div></div>
:ET