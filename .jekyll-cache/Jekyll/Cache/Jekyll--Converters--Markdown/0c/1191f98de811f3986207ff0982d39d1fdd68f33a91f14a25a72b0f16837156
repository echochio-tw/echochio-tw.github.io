I"T<p>寫perl用到 json xml … 記錄一下</p>

<h2 id="perl-hash-dump-">perl HASH Dump …..</h2>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>print_element("Domain",$xml,"");
sub print_element {
  my $name= shift;
  my $element = shift;
  my $indent = shift;
  print $indent . "Element: " . $name. "\n";
  $indent .= "    ";
  foreach my $key (keys %$element) {
    if (ref $element-&gt;{$key} eq "ARRAY") {
      foreach my $subelement (@{$element-&gt;{$key}}) {
        if (ref $subelement eq "HASH") {
          print_element($key, $subelement,$indent);
        } else {
          print $indent . "Element: " . $key . ", Value: " . $subelement . "\n";
        }
      }
    } else {
      print $indent . "Attribute: " . $key . ". Value: " . $element-&gt;{$key} . "\n";
    }
  }
}
</code></pre></div></div>
:ET